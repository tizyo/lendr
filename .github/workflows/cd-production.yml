name: CD — Production

on:
  push:
    branches: [main]

jobs:
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production   # requires manual approval in GitHub

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, bcmath, pdo_mysql, redis, gd, zip

      - name: Install PHP dependencies
        run: composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install npm dependencies & build
        run: |
          npm ci
          npm run build

      - name: Deploy to production via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            cd /var/www/lendr
            git pull origin main
            composer install --no-dev --optimize-autoloader
            php artisan down --retry=60
            php artisan migrate --force
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            php artisan event:cache
            php artisan queue:restart
            php artisan horizon:terminate
            php artisan up
            echo "Production deploy complete $(date)"
            # TODO: Reverb (WebSocket server for real-time broadcasting) has
            # no supervised process on this host yet — Horizon gets restarted
            # above via a process supervisor already managing it; Reverb
            # needs the same (e.g. `php artisan reverb:restart` wired into
            # whatever supervises horizon today) before real-time features
            # will work in production.
